import React from 'react';

type BottonButtonProps = {
  width: number;
  color: string;
  text: string;
};

function BottonButton({ width, color, text }: BottonButtonProps) {
  return <div></div>;
}

export default BottonButton;
